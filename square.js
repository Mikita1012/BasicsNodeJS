let side = 0;
const calculateAreaOfSquare = (side) => side * side;
const calculatePerimeterOfSquare = (side) => 4 * side;

module.exports = {
    calculateAreaOfSquare: calculateAreaOfSquare,
    calculatePerimeterOfSquare: calculatePerimeterOfSquare
}